@using DynamicFormFields.Extensions
@model DynamicFormFields.Models.DynamicFieldWrapper
@{
    using (Html.BeginHtmlFieldPrefixScope(Model.ModelNodePrefix))
    {
        for (var i = 0; i < Model.Items.Length; i++)
        {
            @Html.EditorFor(x => x.Items[i])
        }
    }
}